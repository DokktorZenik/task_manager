spring:
  application:
    name: taskservice

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    hibernate:
      ddl-auto: update


  datasource:
    url: jdbc:postgresql://localhost:5432/task
    username: postgres
    password: postgres
server:
  port: 8081